server:
  port: 8081

# configure postgresql database
spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/vex
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  batch:
    job:
      enabled: false
#    jdbc:
#      initialize-schema: always
